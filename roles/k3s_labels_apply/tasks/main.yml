---
- name: 'Set agent labels'
  become: yes
  delegate_to: '{{ k3s_first_primary }}'
  loop: '{{ agent_labels }}'
  command:
    argv:
      - '/usr/local/bin/kubectl'
      - 'label'
      - 'nodes'
      - '{{ fqdn }}'
      - '{{ item }}'
...
# vim: set filetype=yaml
