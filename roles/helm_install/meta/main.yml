---
dependencies:
  - vars:
      distro_pkgs:
        rhel:
          - '?'  # seems there is no RHEL package for Helm :(
        void:
          - 'kubernetes-helm'
      packages: '{{ distro_pkgs[distro] }}'
    role: 'meta_package_install'
...
# vim: set filetype=yaml
