---
- name: 'Restart keepalived'
  become: yes
  # service:
  #   name: 'keepalived'
  #   state: 'restarted'
  command:
    argv:
      - '{{ service_command }}'
      - 'restart'
      - 'keepalived'

- name: 'Restart sshd'
  become: yes
  # service:
  #   name: 'sshd'
  #   state: 'restarted'
  command:
    argv:
      - '{{ service_command }}'
      - 'restart'
      - 'sshd'
...
# vim: set filetype=yaml
