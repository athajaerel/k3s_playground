---
- name: 'Set agent labels'
  become: yes
  delegate_to: 'k3s-mother-001'
  loop: '{{ agent_labels }}'
  command:
    argv:
      - '/usr/local/bin/kubectl'
      - 'label'
      - 'nodes'
      - '{{ fqdn }}'
      - '{{ item }}'
...
# vim: set filetype=yaml
